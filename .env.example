# OpenAI API Configuration
# Required for agentic and hybrid approaches when LLM intervention is needed
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: OpenAI Organization ID (if you have multiple organizations)
# OPENAI_ORG_ID=your_organization_id_here

# Optional: Model selection (default: gpt-3.5-turbo)
# OPENAI_MODEL=gpt-3.5-turbo

# Optional: Temperature for LLM responses (default: 0.1 for consistency)
# OPENAI_TEMPERATURE=0.1

# Optional: Max tokens per request (default: 1000)
# OPENAI_MAX_TOKENS=1000

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Development Settings
# Set to True to enable debug mode with additional logging
DEBUG=False

# Set to True to enable cost tracking and warnings
COST_TRACKING=True

# Optional: Custom dataset path (default: data/normalization_assessment_dataset_10k.csv)
# DATASET_PATH=path/to/your/dataset.csv
